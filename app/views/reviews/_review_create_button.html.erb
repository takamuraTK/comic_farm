<% if Book.find(book.id).reviewuser.include?(current_user) %>
  <btn class="btn btn-info disabled">既にレビューを書いています</btn>
<% else %>
  <%= link_to 'レビューを書く', review_new_path(book.isbn), class:"btn btn-info" %>
<% end %>